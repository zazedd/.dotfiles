# .dotfiles

## Overview
This repository contains the configurations for my systems.  
Currently, an x86_64-linux machine, nix-darwin macbook, and a homelab server.

## Systems
```sh
darwinConfigurations: darwin (aarch64-darwin)
nixosConfigurations
  ├───lenovo (x86_64-linux)
  └───server (x86_64-linux)
```
